function BootSequence({ onComplete }) {
  return (
    <section className="min-h-screen flex items-center justify-center bg-bg-void">
      <p className="font-mono text-text-secondary">[BootSequence]</p>
    </section>
  )
}

export default BootSequence
